/// Types of events that can be tracked
enum EventType {
  /// Regular custom event
  custom,

  /// Screen view event
  screenView,

  /// Lifecycle event
  lifecycle,

  /// Device event
  device,
}
